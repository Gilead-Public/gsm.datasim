% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-longitudinal.R
\name{assign_schedule_dates}
\alias{assign_schedule_dates}
\title{Join visit-schedule dates onto a subject-visit frame and sort}
\usage{
assign_schedule_dates(
  df,
  visits,
  strDateCol,
  strGroupCol = "subjid",
  strVisitCol = "instancename"
)
}
\arguments{
\item{df}{Data frame of subject-visit records to date.}

\item{visits}{Data frame carrying the visit schedule, with the group and
visit columns plus \code{visit_dt}.}

\item{strDateCol}{Name for the date column in the returned frame -- e.g.
\code{"vs_dt"} for \code{Raw_VS}, \code{"lb_dt"} for a future \code{Raw_LB} adopter.}

\item{strGroupCol, strVisitCol}{Column names identifying the subject and the
visit.}
}
\value{
\code{df} with the date column added, sorted by group then date. The
sort is stable, so repeated records within a visit keep a deterministic
order.
}
\description{
Brings the authoritative visit schedule (\code{Raw_VISIT$visit_dt}) onto a
subject-visit frame and returns it sorted by group then date, so that
"adjacent rows" and "adjacent in time" mean the same thing. Without this,
run adjacency is undefined.
}
\details{
Call this \strong{before} \code{inject_targeted_runs()}; see the ordering contract
documented there.
}
\keyword{internal}
