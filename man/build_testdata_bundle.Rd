% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_testdata_bundle.R
\name{build_testdata_bundle}
\alias{build_testdata_bundle}
\title{Build the shared gsm test data bundle}
\usage{
build_testdata_bundle(
  config = read_testdata_config(),
  output_dir = tempdir(),
  overwrite = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{config}{A \code{testdata_config}, see \code{\link[=read_testdata_config]{read_testdata_config()}}.}

\item{output_dir}{Directory under which the \verb{<study_id>} bundle folder is
created.}

\item{overwrite}{If \code{TRUE}, an existing bundle folder is removed and rebuilt.}

\item{verbose}{Print progress and pipeline output.}
}
\value{
An object of class \code{testdata_bundle}: a list with \code{path} (the bundle
folder), \code{manifest} (the parsed \code{manifest.json}), \code{config}, and \code{study}
(the in-memory \code{longitudinal_study} that was exported).
}
\description{
Reproduces the test data that used to ship inside \code{gsm.core} (\code{lSource} and
the \verb{reporting*} / \verb{analytics*} objects) from a checked-in configuration,
running the mapping, metric, and reporting workflows through \code{workr}, and
writes the result as a versioned bundle:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{<output_dir>/<study_id>/
  manifest.json
  <snapshot_date>/
    raw/          Raw_*.parquet / .csv
    mapped/       Mapped_*.parquet / .csv
    analytics/    Analysis_<metric>_<table>.parquet / .csv
    reporting/    Reporting_*.parquet / .csv
}\if{html}{\out{</div>}}

The two edits the original maintainer script applied silently are explicit,
configurable steps: \code{Raw_SITE$site_status} is forced to
\code{config$post_processing$site_status} on every snapshot, and \code{SnapshotDate}
on the reporting tables is stamped with \code{config$snapshot_dates}, which also
name the snapshot folders.
}
\examples{
\dontrun{
# the production bundle (takes a while)
bundle <- build_testdata_bundle(output_dir = "~/gsm-testdata")

# a reduced bundle for local work
cfg <- read_testdata_config(
  participants = 40, sites = 4, snapshots = 2,
  snapshot_dates = c("2025-02-01", "2025-03-01")
)
bundle <- build_testdata_bundle(cfg, output_dir = tempdir(), overwrite = TRUE)
bundle$manifest$tables
}
}
\seealso{
\code{\link[=read_testdata_bundle]{read_testdata_bundle()}}, \code{\link[=summarize_testdata_bundle]{summarize_testdata_bundle()}},
\code{\link[=explore_testdata]{explore_testdata()}}
}
