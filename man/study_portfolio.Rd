% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_longitudinal_study.R
\name{study_portfolio}
\alias{study_portfolio}
\title{Create a portfolio of studies from a base config and per-study variants}
\usage{
study_portfolio(
  variants,
  participants = 100,
  sites = 10,
  snapshots = 6,
  interval = "1 month",
  domains = c("AE", "LB", "VISIT"),
  ...
)
}
\arguments{
\item{variants}{Named list of per-study override lists.  Names become the
study identifiers.  Each element may contain any combination of:
\code{participants}, \code{sites}, \code{snapshots}, \code{interval}, \code{domains},
\code{outlier_intensity}, \code{run_analytics}, \code{run_reporting}, or any other
argument accepted by \code{\link[=create_longitudinal_study]{create_longitudinal_study()}}.}

\item{participants}{Default participant count for studies that do not specify
their own (default 100).}

\item{sites}{Default site count (default 10).}

\item{snapshots}{Default number of snapshots (default 6).}

\item{interval}{Default snapshot interval (default \code{"1 month"}).}

\item{domains}{Default domain vector (default \code{c("AE", "LB", "VISIT")}).}

\item{...}{Additional arguments forwarded verbatim to
\code{\link[=create_multiple_longitudinal_studies]{create_multiple_longitudinal_studies()}} (e.g. \code{run_analytics},
\code{run_reporting}, \code{parallel}, \code{verbose}).}
}
\value{
A \code{multiple_longitudinal_studies} object (named list of
\code{longitudinal_study} objects).
}
\description{
A convenience wrapper around \code{\link[=create_multiple_longitudinal_studies]{create_multiple_longitudinal_studies()}} that lets
you define a shared base configuration and a single named list of per-study
overrides (variants).  Only the fields that differ between studies need to
appear in each variant entry -- everything else falls back to the base.
}
\details{
Vectorised parameters (\code{participants}, \code{sites}, \code{snapshots},
\code{outlier_intensity}, \code{interval}) are automatically extracted from the variant
list so you never have to maintain a parallel vector alongside \code{study_configs}.
}
\examples{
\dontrun{
# Define shared defaults; each variant only specifies what changes
studies <- study_portfolio(
  variants = list(
    "PHASE2-SMALL" = list(
      participants = 80, sites = 8, snapshots = 4,
      domains = c("AE", "LB")
    ),
    "PHASE3-LARGE" = list(
      participants = 400, sites = 25, snapshots = 12,
      domains = c("AE", "LB", "VISIT", "PD")
    ),
    "SAFETY-RUN" = list(
      participants = 50, sites = 3, snapshots = 8,
      outlier_intensity = 2
    )
  ),
  # Shared defaults (used when a variant does not override)
  participants = 100,
  sites = 10,
  snapshots = 6,
  interval = "1 month",
  domains = c("AE", "LB", "VISIT"),
  run_analytics = FALSE,
  verbose = TRUE
)

names(studies) # "PHASE2-SMALL" "PHASE3-LARGE" "SAFETY-RUN"
studies[["PHASE3-LARGE"]]$config # inspect per-study config
}
}
