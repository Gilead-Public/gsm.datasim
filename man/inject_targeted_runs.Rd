% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-longitudinal.R
\name{inject_targeted_runs}
\alias{inject_targeted_runs}
\title{Inject consecutive runs to hit a target repeat rate}
\usage{
inject_targeted_runs(values, groups, dTargetRate, nWindowLength = 3)
}
\arguments{
\item{values}{Numeric vector of measurements, ordered within group.}

\item{groups}{Vector the same length as \code{values} identifying each value's
group (typically subject).}

\item{dTargetRate}{Target repeat rate in \verb{[0, 1]}.}

\item{nWindowLength}{Whole number \verb{>= 2}. Rolling window length.}
}
\value{
\code{values} with runs injected, carrying a \code{"realized"} attribute: a
list with the achieved \code{numerator} and \code{denominator}.
}
\description{
Overwrites contiguous blocks of \code{values} within each group so that the
proportion of length-\code{nWindowLength} rolling windows whose values are all
identical is approximately \code{dTargetRate}. The construction is deterministic
given the group sizes -- the rate is \emph{built}, not sampled.
}
\details{
Rows must be \strong{already ordered within group} by the key the downstream
metric will order on; this function injects over contiguous positions and
does not sort. \code{NA} values are never overwritten and are excluded from the
window counts. Small groups quantize, since the target numerator is
\code{round(dTargetRate * D)}.
}
\keyword{internal}
