% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-longitudinal.R
\name{utils-longitudinal}
\alias{utils-longitudinal}
\title{Longitudinal data helpers}
\description{
Domain-neutral helpers for shaping longitudinal (subject x visit) data so it
can exercise sequence-based metrics such as the repeat-measure KRIs
(\code{kri0020a-f}). They operate on plain vectors and data frames, and know
nothing about vitals, sites, or any particular \verb{Raw_*} domain.
}
\details{
The intended composition is \strong{sort first, then inject}:
\code{assign_schedule_dates()} puts records in the order the downstream metric
will use, and \code{inject_targeted_runs()} then writes runs over contiguous
positions. Reversing that order silently produces runs that are adjacent in
row order but not in time, which the metric will not count.
}
\keyword{internal}
