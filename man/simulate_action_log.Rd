% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/action_log_simulation.R
\name{simulate_action_log}
\alias{simulate_action_log}
\title{Simulate an ActionLog history}
\usage{
simulate_action_log(
  df_results,
  state_probabilities = c(`Awaiting Triage` = 0.2, `No Action` = 0.4, `Open Action` =
    0.2, `Closed Action` = 0.2),
  transition_matrix = NULL,
  missing_probability = 0,
  duplicate_probability = 0,
  seed = NULL,
  extraction_date = NULL,
  work_item_id_start = 900000L,
  organization = "Gilead-RND-CDS-RBQM",
  include_intermediates = FALSE
)
}
\arguments{
\item{df_results}{Data frame containing \code{StudyID}, \code{SnapshotDate},
\code{GroupLevel}, \code{GroupID}, \code{MetricID}, and \code{Flag}. Optional display and action
columns override generated defaults.}

\item{state_probabilities}{Named probabilities for the four ActionLog states.}

\item{transition_matrix}{Optional state-by-state transition probability
matrix used for repeated group/metric findings across snapshots.}

\item{missing_probability}{Probability that an eligible finding has no ADO
work item.}

\item{duplicate_probability}{Probability that an eligible work item is
duplicated within its snapshot.}

\item{seed}{Optional random seed. The caller's random-number state is
restored after generation.}

\item{extraction_date}{Date represented by the synthetic extraction. Defaults
to seven days after the latest snapshot.}

\item{work_item_id_start}{First synthetic ADO work item ID.}

\item{organization}{Azure DevOps organization used only to construct URLs.}

\item{include_intermediates}{If \code{TRUE}, return raw work items, the tabulated
work item data, and the augmented ActionLog in a named list.}
}
\value{
An ActionLog data frame, or a named list of all three schema layers.
}
\description{
Creates raw ADO-compatible work items, tabulates them with \code{grail.ado}, and
augments them with \code{grail}. This convenience function can return the final
ActionLog alone or all three intermediate schema layers.
}
