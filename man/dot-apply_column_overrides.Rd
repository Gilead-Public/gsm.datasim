% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_data_from_workflows.R
\name{.apply_column_overrides}
\alias{.apply_column_overrides}
\title{Apply Column Overrides to a Generated Domain Data Frame}
\usage{
.apply_column_overrides(df, domain, column_overrides)
}
\description{
Post-processes a domain \code{data.frame} by applying user-supplied column
specifications from \code{column_overrides}. Columns may be added (if new) or
replaced (if already present).
}
\details{
Each column value in the override list can be:
\itemize{
\item A \strong{function} with signature \verb{function(n, df)} — receives row count and the
full domain \code{data.frame}; useful for deriving values from other columns.
\item A \strong{function} with signature \verb{function(n)} — receives only the row count.
\item A \strong{vector} — sampled with replacement to \code{n} rows.
\item A \strong{scalar} — repeated to fill all \code{n} rows.
}
}
\keyword{internal}
