% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_testdata.R
\name{explore_testdata}
\alias{explore_testdata}
\title{Test Data Explorer widget}
\usage{
explore_testdata(
  bundle,
  max_rows = 1000,
  full_layers = "raw",
  full_max_rows = 5000,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{bundle}{A \code{testdata_bundle} or the path to a bundle folder.}

\item{max_rows}{Maximum number of rows embedded for tables that are not in
\code{full_layers} of the latest snapshot.}

\item{full_layers}{Layers of the latest snapshot that get the larger
\code{full_max_rows} budget.}

\item{full_max_rows}{Maximum number of rows embedded for tables in
\code{full_layers} of the latest snapshot.}

\item{width, height}{Widget size (CSS units); defaults fill the container.}

\item{elementId}{Optional element id for the widget.}
}
\value{
An htmlwidget of class \code{Widget_TestDataExplorer}.
}
\description{
An interactive htmlwidget that summarises a test data bundle and lets users
browse every table in it: a header built from \code{manifest.json}, an inventory
tree (snapshot, layer, table with rows and columns), a sortable, searchable,
paginated table viewer with per-column summaries, and a few overview charts
(rows per table across snapshots, subjects per site, adverse events per
subject, enrolment over time).
}
\details{
The table viewer and tree are adapted from the \code{open.gismo} / \code{workr}
front-end modules and charts use \code{gsm.viz}; see
\code{inst/htmlwidgets/lib/testdata-explorer/VENDORED.md}.

Tables in \code{full_layers} of the latest snapshot (by default the \verb{raw/} layer,
i.e. the \code{lSource} equivalent) embed up to \code{full_max_rows} rows; every other
table is capped at \code{max_rows} rows so the page stays small (a full-size
bundle's \code{Raw_LB} alone would be tens of megabytes). Summary statistics
always cover the full tables, and the header links to the complete bundle.
}
\examples{
\dontrun{
bundle <- read_testdata_bundle("~/gsm-testdata/AA-AA-000-0000")
explore_testdata(bundle)
}
}
