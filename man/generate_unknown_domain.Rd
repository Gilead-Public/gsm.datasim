% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/column_type_generator.R
\name{generate_unknown_domain}
\alias{generate_unknown_domain}
\title{Generate Data for an Unknown Domain}
\usage{
generate_unknown_domain(
  domain_name,
  domain_spec,
  n,
  context = list(),
  previous_data = NULL
)
}
\arguments{
\item{domain_name}{Character. The domain name (e.g. \code{"Raw_CUSTOM"}).}

\item{domain_spec}{Named list. Column specifications as returned by
\code{CombineSpecs()} for this domain.}

\item{n}{Integer. Target total number of rows for this snapshot (cumulative).}

\item{context}{List with \code{data}, \code{start_date}, \code{end_date} (same structure as
passed to \code{\link[=generate_column_by_type]{generate_column_by_type()}}).}

\item{previous_data}{Optional \code{data.frame} from the prior snapshot for this
domain. When provided, existing rows are retained and only the delta
(\code{n - nrow(previous_data)}) new rows are generated.}
}
\value{
A \code{data.frame} with \code{n} rows and one column per spec entry (or more
rows if \code{previous_data} already exceeds \code{n}).
}
\description{
Creates a complete \code{data.frame} for a domain that has no dedicated generator
function in the domain registry or as a legacy \verb{Raw_*()} function. Each column
in the spec is generated using \code{\link[=generate_column_by_type]{generate_column_by_type()}}, with an attempt to
reuse existing named generator functions for columns whose names match a
known generator (e.g. \code{subjid}, \code{studyid}).
}
\details{
When \code{previous_data} is supplied (a \code{data.frame} from the prior snapshot),
the function uses a cumulative delta pattern: it keeps all existing rows and
only generates \code{n - nrow(previous_data)} new rows, then binds them together.
This mirrors the behavior of the core domain registry generators for
longitudinal multi-snapshot generation.
}
